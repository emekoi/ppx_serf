(executable
  (name animechan)
  (preprocess (pps ppx_serf lwt_ppx))
  (libraries ppx_deriving_yojson cohttp cohttp-lwt-unix lwt_ssl stringext extlib)
)

(alias
  (name quote)
  (deps animechan.exe))
